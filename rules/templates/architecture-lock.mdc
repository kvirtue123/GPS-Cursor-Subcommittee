---
description: Always-on architecture lock for a single component/service (e.g. an MCP server, a library, a worker). Encodes the non-obvious constraints an agent would otherwise "fix" into breakage.
globs: **/*
---

# {{COMPONENT_NAME}} — Development Rules

Always-on guardrails for this component. The point is to record the **counter-intuitive** decisions so an agent doesn't refactor them away.

## Architecture
- {{PRIMARY_PATH}} uses {{APPROACH_A}} — NOT {{TEMPTING_BUT_WRONG_ALTERNATIVE}}
- {{SECONDARY_PATH}} uses {{APPROACH_B}} because {{REASON}}
- All {{UNIT}}s implement the `{{INTERFACE_NAME}}` interface from `{{TYPES_FILE}}`
- {{CROSS_CUTTING_CONCERN}} is {{IMPLEMENTATION}} (e.g. cache = SQLite, 24h TTL)

## Key Files
- `{{MOST_CRITICAL_FILE}}` — Most critical file. {{WHY}}.
- `{{ENTRY_FILE}}` — {{ROLE}}. Thin wiring only.

## Testing
Always verify changes with:
```bash
{{VERIFY_COMMAND}}
```

## Important (the non-obvious stuff)
- {{EXTERNAL_DEPENDENCY}} changes ~{{FREQUENCY}}; the code self-heals by {{SELF_HEAL_MECHANISM}}.
- {{GOTCHA_1}}
- {{RUNTIME_CONSTRAINT}} (e.g. "Node 18–24 only; 25+ breaks native module X").
- **Never {{FORBIDDEN_ACTION}}** — {{CONSEQUENCE}} (e.g. "browser automation here — Locker Service detects CDP").

<!-- Provenance: pattern from project `FHA Call Center` (sf-docs-mcp/.cursor/rules/core.mdc + AGENTS.md). Canonical example: rules/canonical/sf-docs-mcp.core.mdc. -->
