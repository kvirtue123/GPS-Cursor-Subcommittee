---
description: Agent goal statement and document map. Genericized template — replace placeholders and @-references with your project's docs.
globs: force-app/main/default/**/*,docs/**/*
alwaysApply: false
---

# {{PROJECT_NAME}} — Goals & Document Map

## Agent Goal Statement
{{ONE_PARAGRAPH_GOAL}} — state, in plain language, what the agent is for: the primary outcome, the secondary value, and any tertiary scope. Keep it to a few sentences.

## Quick Reference
- @{{KNOWLEDGE_REF}} — agent spec reference: subagent registry, action inventory, FSM state register, Lightning Type registry, token rules, reuse registry
- @{{AGENT_SCRIPT_OVERVIEW}} — Agent Script DSL entry point (read subsequent pages in order as needed)

## Document Map
When looking for specific design decisions, refer to:
- **Topics, Actions, Global Instructions, Assumptions**: @{{DESIGN_A}}
- **Percepts, FSM transitions, Business rules, Metrics**: @{{DESIGN_B}}
- **Object/field specs, picklists, validation rules, ERD**: @{{DESIGN_C}}
- **Master consolidated doc (authoritative)**: @{{MASTER_DESIGN}}
- **Implementation guide (follow — not for design decisions)**: @{{BUILD_PHASE_1}}, @{{BUILD_PHASE_N}}

> Keep `@`-references accurate. A stale path is worse than no map — it sends the agent to read the wrong (or missing) file.
