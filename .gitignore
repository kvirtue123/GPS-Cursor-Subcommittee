# Internal discovery/planning material — contains customer names
discovery/

# Internal catalog — source of truth for weekly releases, contains customer names
CATALOG-INTERNAL.md

# Canonical files contain customer project names — provenance only, not public install targets
rules/canonical/
patterns/canonical/

# macOS
.DS_Store
